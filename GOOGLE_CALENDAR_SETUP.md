# Googleカレンダー連携設定ガイド

このガイドでは、Mobilis予約システムとGoogleカレンダーの双方向同期を設定する方法を説明します。

## 前提条件

- Googleアカウント
- Google Cloud Consoleへのアクセス権限

## 設定手順

### 1. Google Cloud Consoleでプロジェクトを作成

1. [Google Cloud Console](https://console.cloud.google.com/)にアクセス
2. 新しいプロジェクトを作成（または既存のプロジェクトを選択）
3. プロジェクト名を入力して「作成」をクリック

### 2. Calendar APIを有効化

1. 左側のメニューから「APIとサービス」→「ライブラリ」を選択
2. 「Google Calendar API」を検索
3. 「Google Calendar API」を選択して「有効にする」をクリック

### 3. OAuth認証情報を作成

1. 「APIとサービス」→「認証情報」を選択
2. 「認証情報を作成」→「OAuth クライアント ID」を選択
3. アプリケーションの種類で「デスクトップアプリ」を選択
4. 名前を入力（例: "Mobilis Calendar Sync"）
5. 「作成」をクリック
6. 表示された「クライアント ID」と「クライアント シークレット」をメモ

### 4. OAuth同意画面の設定（必須）

**重要**: OAuth同意画面の設定は必須です。ただし、内部使用やテストユーザーのみの場合は、Googleの検証プロセスは不要です。

1. 「OAuth同意画面」タブを選択
2. **ユーザータイプを選択**：
   - **内部使用のみの場合**: 「内部」を選択（Google Workspaceアカウントが必要）
   - **外部ユーザーも使用する場合**: 「外部」を選択（推奨：テストユーザーを追加すれば検証不要で使用可能）
3. アプリ情報を入力：
   - **アプリ名**: 例「Mobilis予約システム」
   - **ユーザーサポートメール**: あなたのメールアドレス
   - **デベロッパーの連絡先情報**: あなたのメールアドレス
4. 「スコープを追加または削除」をクリック
5. **スコープの追加方法**：
   - 「スコープを追加または削除」画面で、「スコープを追加」ボタンをクリック
   - 検索ボックスに「calendar」と入力
   - 表示されたスコープ一覧から `https://www.googleapis.com/auth/calendar` を探す
   - チェックボックスにチェックを入れる
   - 「更新」ボタンをクリック
   - **注意**: 複数のスコープが表示される場合がありますが、`https://www.googleapis.com/auth/calendar` を選択してください
6. 「保存して次へ」をクリック
7. **テストユーザーを追加**（重要）：
   - 「保存して次へ」をクリックすると、次の画面（「テストユーザー」または「ユーザー」タブ）が表示されます
   - 画面の上部または中央に「**+ ADD USERS**」または「**テストユーザーを追加**」というボタンがあります
   - このボタンをクリック
   - 表示された入力欄に、Googleカレンダーにアクセスするアカウントのメールアドレスを入力
   - 「追加」または「保存」ボタンをクリック
   - 複数のアカウントを使用する場合は、同じ手順で追加（1つずつ追加するか、複数同時に追加できる場合があります）
   - **注意**: テストユーザーとして追加されたアカウントのみが認証できます
8. 「ダッシュボードに戻る」または「保存して次へ」をクリックして完了

**補足**:
- テストユーザーとして追加すれば、Googleの検証プロセスなしで使用できます
- 一般公開する場合は、Googleの検証プロセスが必要です（通常は不要）

### 5. 認証情報ファイルのダウンロード

**重要**: OAuthクライアントIDを作成した後、認証情報ファイルをダウンロードする必要があります。

**注意**: Googleはセキュリティ上の理由で、クライアントシークレットを一度表示した後は再表示しません。作成直後にダウンロードまたはコピーしてください。

#### 方法1: 作成直後にダウンロード（推奨）

1. OAuthクライアントIDを作成した直後の画面で、**「JSONをダウンロード」**ボタンが表示されます
2. このボタンをクリックしてJSONファイルをダウンロード
3. ファイル名を `google_calendar_credentials.json` に変更
4. プロジェクトの `config/` ディレクトリに配置

#### 方法2: クライアントシークレットが表示されない場合

クライアントシークレットが表示されない、またはダウンロードできない場合は、**新しいOAuthクライアントIDを作成**してください：

1. 「APIとサービス」→「認証情報」ページに移動
2. 「認証情報を作成」→「OAuth クライアント ID」を選択
3. アプリケーションの種類で「デスクトップアプリ」を選択
4. 名前を入力（例: "Mobilis Calendar Sync 2"）
5. 「作成」をクリック
6. **作成直後の画面で必ず「JSONをダウンロード」ボタンをクリック**してJSONファイルをダウンロード
7. ファイル名を `google_calendar_credentials.json` に変更
8. プロジェクトの `config/` ディレクトリに配置

**重要**: 
- クライアントシークレットは作成時に一度だけ表示されます
- 画面を閉じる前に必ずダウンロードまたはコピーしてください
- 既存のクライアントIDに新しいシークレットを追加することもできますが、新しいクライアントIDを作成する方が簡単です

### 6. 環境変数の設定

`.env` ファイルまたはHerokuの環境変数に以下を追加：

```bash
GOOGLE_CALENDAR_SYNC_ENABLED=true
```

### 7. データベースマイグレーションの実行

```bash
rails db:migrate
```

### 8. Gemのインストール

```bash
bundle install
```

以下のgemがインストールされます：
- `google-apis-calendar_v3`
- `googleauth`

### 9. 認証の実行

1. 管理画面にログイン
2. 「Googleカレンダー連携設定」ページにアクセス
3. 「認証を開始」ボタンをクリック
4. Googleアカウントでログイン
5. アクセス権限を許可
6. 認証コードをコピー（表示された場合）
7. 認証が完了すると、`config/google_calendar_token.yaml` が作成されます

## 使用方法

### 自動同期

環境変数 `GOOGLE_CALENDAR_SYNC_ENABLED=true` が設定されている場合、以下の操作で自動的にGoogleカレンダーと同期されます：

- **予約作成時**: Googleカレンダーにイベントを作成
- **予約更新時**: Googleカレンダーのイベントを更新
- **予約キャンセル時**: Googleカレンダーからイベントを削除

### 手動同期

管理画面の「Googleカレンダー連携設定」ページから：

1. **予約 → Googleカレンダー**: システム内の予約をGoogleカレンダーに送信
2. **Googleカレンダー → 予約**: Googleカレンダーのイベントを予約として取り込み
3. **接続テスト**: Googleカレンダーとの接続をテスト

## トラブルシューティング

### 認証エラーが発生する

- `config/google_calendar_credentials.json` が正しく配置されているか確認
- **OAuth同意画面の設定が完了しているか確認**（必須）
- **テストユーザーが追加されているか確認**（重要：認証に使用するGoogleアカウントがテストユーザーに追加されている必要があります）
- OAuth同意画面で「外部」を選択した場合、認証に使用するアカウントがテストユーザーリストに含まれている必要があります

### 同期が動作しない

- 環境変数 `GOOGLE_CALENDAR_SYNC_ENABLED=true` が設定されているか確認
- `config/google_calendar_token.yaml` が存在するか確認
- ログファイルでエラーメッセージを確認

### トークンの有効期限切れ

Googleカレンダーのアクセストークンは一定期間で期限切れになります。期限切れの場合は、再度認証を実行してください。

## セキュリティに関する注意事項

- `google_calendar_credentials.json` と `google_calendar_token.yaml` は機密情報です
- これらのファイルをGitリポジトリにコミットしないでください
- `.gitignore` に以下を追加することを推奨：

```
config/google_calendar_credentials.json
config/google_calendar_token.yaml
```

## 参考リンク

- [Google Calendar API ドキュメント](https://developers.google.com/calendar/api)
- [Google OAuth 2.0 ドキュメント](https://developers.google.com/identity/protocols/oauth2)

