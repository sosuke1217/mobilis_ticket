<%= turbo_stream.remove "ticket_#{@ticket_id}" %>
<% if @user.tickets.where("remaining_count > 0").none? %>
  <%= turbo_stream.update "active_ticket_section" do %>
    <div class="p-3">
      <p class="text-muted">使用可能なチケットはありません。</p>
    </div>
  <% end %>
<% end %>